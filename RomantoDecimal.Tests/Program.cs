using System;
using FluentAssertions;
using Xunit;

namespace RomantoDecimal.Tests
{
  public class Program
  {
    [Fact]
    public void Should_returnDecimalInt()
    {
      // //Assemble
      // string romanNum = "X";
      // //Act
      // int integerNum = ConvertRoman(romanNum);
      // //Assert
      // integerNum.Should().Be(10);
    }
  }
}
